import { View } from 'react-native';

import { AppText } from '@/src/shared/components/AppText';

export default function AnalysisRoute() {
  return (
    <View>
      <AppText>分析功能将在第二阶段开放。</AppText>
    </View>
  );
}
